# Copyright (c) 2026 CueCrux Ltd.
# Licensed under the Apache License, Version 2.0.
# See LICENSE in the repository root.

[package]
name = "crux-cost"
publish.workspace = true
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Ground-truth token-burn cost lens: parses a Claude Code transcript for measured usage, attributes carried context cost per source, and generates reduction levers. Shared CostReport contract for the CLI (producer) and daemon (reader)."

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[lints]
workspace = true
