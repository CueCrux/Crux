[package]
name = "corecrux-index"
publish.workspace = true
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
blake3 = { workspace = true }
# `.ccxe` reads f16-quantised vectors and mmaps large companions. Both arrive with
# the ported dense-companion format — see VENDORED_FROM.md.
half = "2.7.1"
memmap2 = "0.9.10"
thiserror = { workspace = true }
xxhash-rust = { workspace = true, features = ["xxh64"] }

corecrux-frame = { path = "../corecrux-frame" }
corecrux-segment = { path = "../corecrux-segment" }

[dev-dependencies]
proptest = { workspace = true }
tempfile = { workspace = true }

[lints]
workspace = true
