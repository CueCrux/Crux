# Non-test unwrap()/expect() baseline per crate.
# Initially captured 2026-07-10 at commit 59e9f26 (main); refreshed 2026-07-11
# 2026-07-12 +11 corecruxd: all in #[cfg(test)] code for the async repo-scan
# feature (PR #373) — job-id/status assertions in http/tests.rs and registry
# decode/recovery tests; zero production-path additions (verified by diff).
# post-merge of main (10 corecruxd sites from pre-gate PRs #354-#360); lower counts are
# ratcheted in place when sites are removed.
# 2026-07-13 +1 corecruxd: one #[cfg(test)] assertion in http/tests.rs for the
# legal-hold integrity tests (P1 Track B, PR #393) — reserved-prefix / override
# attribution test setup; zero production-path additions (verified by diff).
# 2026-07-15 +14 corecruxd: real-keypair sync mutual-auth handshake tests in
# http/tests.rs (audit M2b, PR #408) — token/keypair construction + header
# assembly in the 5 sync_mutual_auth_* tests; zero production-path additions
# (the require_sync_peer / nonce handler paths use no unwrap/expect; verified by diff).
# 2026-07-15 +10 corecruxd: OD-37 / audit-v2 closeout M1 write-tenant stamping
# tests — the tenant_write_stamp_isolates_reads_end_to_end_m1 e2e test in
# http/tests.rs (JWT/body/response assertions). Zero production-path additions:
# the resolvers (resolve_write_tenant_flagged / resolve_read_tenant_flagged) and
# facts.rs write/read hooks use Result/Option, no unwrap/expect (verified by diff).
# Method: count lines containing .unwrap()/.expect( in crates/<crate>/**/*.rs,
#   excluding */tests/* paths and lines after an inline #[cfg(test)] marker.
# Consumer: scripts/unwrap-ratchet.sh (M5 of ExecPlan
#   crux-external-findings-remediation-2026-07-10). CI fails if any crate
#   exceeds its baseline; lowering a count updates this file in the same PR.
# 2026-07-16 +3 corecruxd: audit-v2 M3 revoked-peer HTTP test in http/tests.rs
#   (entity upsert + serde_json assertions seeding a revoked identity link);
#   zero production-path additions (require_sync_peer uses Result/Option, no unwrap).
# 2026-07-17 +3 corecruxd: PR #423 P2/P1 HTTP tests in http/tests.rs — the
#   min_effective_confidence floor test + console review-queue/expiry tests
#   (serde_json response-body assertions). tests.rs is #[cfg(test)] via an
#   out-of-file #[path] mod decl so the ratchet counts its unwraps; zero
#   production-path additions (the recall floor uses Option, no unwrap).
# 2026-07-22 +3 corecruxd: passport-mint approval HTTP fixture setup in
#   http/tests.rs (system-time, JWT encoding, and Authorization header
#   construction). tests.rs is #[cfg(test)] via the out-of-file module above;
#   zero production-path additions (verified by diff).
# 2026-07-22 +17 corecruxd: macaroon M3′ recipient-bound delegation HTTP tests in
#   http/tests.rs (sync-delegation base/token/header builders + 8 adversarial
#   gate tests) — #[cfg(test)] only, counted because tests.rs is an out-of-file
#   #[path] mod; zero production-path additions (require_sync_peer /
#   verify_sync_delegation use Result/Option, no unwrap/expect).
# 2026-07-28 +4 corecruxd, +39 crux-mcp: all in examples/ measurement harnesses
# for the runtime code-map programme — corecruxd/examples/symbol_resolve_gate.rs
# (the M1 resolution gate) and crux-mcp/examples/code_intel_{control,treatment,
# score}.rs (the M0/M2/M3 benchmark arms). Every one carries
# #![allow(clippy::expect_used, clippy::unwrap_used)] and follows the existing
# examples/token_bench.rs precedent: a benchmark that panics on malformed input
# is correct — the alternative is a harness that swallows an error and reports a
# number that is quietly wrong. ZERO production-path additions in either crate
# (verified by diff: crux-mcp/src/tools/code_intel.rs adds 0 sites).
# Observation for the operator, deliberately NOT acted on here: count_sites()
# excludes */tests/* but not */examples/*. Widening that exclusion would reduce
# the ratchet's coverage for every crate, so it is a decision to take on its own
# merits rather than a side effect of making this PR pass.
# 2026-07-28 +1 corecruxd: one `.expect("seed repo")` in the repo-allowance test
# seed helper in http/tests.rs (crux-code-intel-pro-hosted-surface M1). #[cfg(test)]
# code, counted only because tests.rs is an out-of-file #[path] mod — the same case
# as the 2026-07-12 and 2026-07-15 entries above. Zero production-path additions:
# repo_allowance.rs contains no unwrap()/expect() at all; its two uses are
# `unwrap_or`, which the scan does not match and which cannot panic. A seed helper
# that swallowed a store error would assert against wrong data, so failing loudly
# is correct — contorting it to dodge the counter would be the wrong trade.
# 2026-07-28 +7 corecruxd: the M2 adversarial tenant-isolation suite in
# http/tests.rs (crux-code-intel-pro-hosted-surface M2) — JWT minting and header
# parsing in the token helpers (`.expect("jwt")`, `.parse().unwrap()`) plus the
# SystemTime reads. #[cfg(test)] code, counted only because tests.rs is an
# out-of-file #[path] mod. Zero production-path additions: this commit adds no
# non-test code at all. The suite must mint real HS256 tokens because DevScopes
# sets TenantAllow::Any — an isolation test written in that mode would pass
# against a daemon with no isolation whatsoever, so the token plumbing is the
# part that makes the test meaningful.
corecrux-frame 0
corecrux-index 3
corecrux-memory 0
corecrux-projections 51
corecrux-proto 0
corecrux-receipts 85
corecrux-retrieval 2
corecrux-segment 32
corecrux-storage 48
corecrux-types 1
corecruxctl 9
# 2026-07-25 +55 corecruxd: PR #519 integrations+library HTTP fixtures in
# http/tests.rs only (extensions registry list 4, audit-trail keyring round-trip,
# studio library GET/install family 10 incl. locally-served signed index + pack
# servers) — all #[cfg(test)]-gated via mod.rs; zero production-path additions
# (verified per-file against origin/main: every non-tests.rs delta is 0).
# 2026-07-28 +3 corecruxd: GET /v1/console/connections token-non-disclosure
# tests in http/tests.rs (payload serialises x2, fingerprint present) — that
# file carries no in-file #[cfg(test)] attribute (it is gated from mod.rs), so
# the scan counts it. The sibling console.rs assertion added by the same PR sits
# after an in-file #[cfg(test)] and is correctly excluded. Zero production-path
# additions (verified per-file against origin/main).
corecruxd 864
crux-claude-hooks 1
crux-config-wizard 8
crux-contrib 0
crux-cost 0
crux-enterprise-shim 0
crux-integration-tests 7
crux-integrations 0
crux-lens-features 0
crux-mcp 153
crux-observe-api 0
crux-observe 9
crux-router 0
crux-session 1
crux-sync 0
rcx-capability-token 0
vaultcrux-local 0
