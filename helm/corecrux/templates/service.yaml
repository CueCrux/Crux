apiVersion: v1
kind: Service
metadata:
  name: {{ include "corecrux.fullname" . }}
  labels:
    {{- include "corecrux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "corecrux.selectorLabels" . | nindent 4 }}
