{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Crux desktop shell. The shell is a thin webview over the loopback sidecar console; it exposes no bespoke JS->core commands, so only the framework defaults plus the dialog plugin (used for the boot-failure dialog) are granted.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
