{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Zero-IPC capability for the Crux console webview. There is deliberately no remote URL grant and no command permission; profile, credential, lifecycle, tray, and navigation work stays in native Rust.",
  "windows": ["main"],
  "permissions": []
}
